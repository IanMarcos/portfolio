{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "portfolio",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "typecheck": "tsc --noEmit",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public -f",
    "deploy:ci": "gatsby build --prefix-paths && gh-pages -d public -f -r https://$GH_TOKEN@github.com/IanMarcos/portfolio.git"
  },
  "dependencies": {
    "gatsby": "^5.12.12",
    "gatsby-plugin-google-fonts": "^1.0.1",
    "gatsby-plugin-sass": "^6.12.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.0.1",
    "sass": "^1.69.5"
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "@types/react": "^18.2.14",
    "@types/react-dom": "^18.2.6",
    "gatsby-plugin-root-import": "^2.0.9",
    "gh-pages": "^6.1.1",
    "typescript": "^5.1.6"
  }
}
